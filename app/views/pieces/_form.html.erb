<%= form_tag(game_piece_path(game_id: @game.id, id: @piece.id), :method => :put) do %>
  <%= hidden_field_tag :x_pos, pos[0] %>
  <%= hidden_field_tag :y_pos, pos[1] %>
  <%= submit_tag '', class: "button" %>
<% end %>
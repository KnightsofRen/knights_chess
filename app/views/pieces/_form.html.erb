<%= simple_form_for @piece, :url => game_piece_path(game_id: @game.id, id: @piece.id), :method => :put do |f| %>
  <%= f.hidden_field :x_coordinate, value: pos[0] %>
  <%= f.hidden_field :y_coordinate, value: pos[1] %>
  <%= f.submit '', :class => "button" %>
<% end %>

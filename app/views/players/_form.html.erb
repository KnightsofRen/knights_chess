<%= form_tag(delete_user_players_path, :method => :put) do %>
  <%= hidden_field_tag :user_to_delete, u.id %>
  <%= submit_tag 'x', data: { confirm: "Are you sure you want to delete #{u.username}?" }, class: "btn btn-danger btn-sm" %>
<% end %>